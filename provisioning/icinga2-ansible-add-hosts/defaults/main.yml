---
# defaults file for icinga2-ansible-add-hosts

icinga2_hosts_dir: "/etc/icinga2/conf.d/"
host_address: "{{ hostvars[item]['ansible_enp0s8']['ipv4']['address'] }}"
host_attributes: "none"
host_checks: "none"
